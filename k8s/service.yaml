apiVersion: v1
kind: Service
metadata:
  name: sqlite-api
  namespace: job-search
  labels:
    app: sqlite-api
spec:
  type: NodePort
  selector:
    app: sqlite-api
  ports:
  - port: 8000
    targetPort: 8000
    nodePort: 30080
